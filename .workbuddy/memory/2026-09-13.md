# 2026-09-13 工作日志

## 任务：前端转 AI Agent 开发 — 生成完整学习包

### 交付位置
`/Users/kevinzzzzzz/Documents/code/AIAPP_learning/AI-Agent转型学习包/`

### 交付内容（63 个文件，808KB）
- **总纲 3 份**：README 总览、12周学习路线与验收标准、环境搭建与工具链指南
- **模块文档 10 份**：模块01 LLM基础 → 模块10 部署生产化，约 20 万字
- **可运行项目 4 个**：约 4500 行代码
  - 01-cli-chatbot：流式对话、多轮上下文、token 统计
  - 02-rag-doc-assistant：手写向量库+BM25+RRF融合+引用溯源
  - 03-tool-agent：手写 ReAct 循环+工具注册表+安全沙箱+人在环
  - 04-agent-saas：FastAPI+SSE 后端 + 单文件 HTML 前端
- **速查资源 3 份**：术语速查表、学习资源清单、面试题库（87 题）

### 关键决策
1. **技术栈**：Python 为主 + 前端做 UI（岗位需求最大，且能复用用户前端经验）
2. **节奏**：12 周，每周一个可验证产出
3. **项目刻意不用 LangChain/LangGraph 主实现** —— 手写一遍理解本质，另附 LangGraph 对照版（`03-tool-agent/src/langgraph_version.py`）
4. **前端用单文件 HTML 而非 Next.js** —— 学习重点是 SSE 协议本身，避免框架封装掩盖细节

### 已核实的技术事实（2026-09-13，写入 README 时效声明）
- DeepSeek：`deepseek-chat`/`deepseek-reasoner` 已于 2026-07-24 下线，现用 `deepseek-v4-flash`/`deepseek-v4-pro`
- LangGraph 1.0 已废弃 `langgraph.prebuilt.create_react_agent`，改用 `langchain.agents.create_agent`
- Vercel AI SDK v5：`useChat` 从 `@ai-sdk/react` 导入，改用 transport 模式
- MCP 最新规范 2026-07-28
- 百炼 `text-embedding-v4`：单次最多 10 条（最常见的 400 错误来源）

### 测试与修复（重要）
用假向量/假模型做了离线测试，**不消耗 API 额度**，真实修出 3 个缺陷：
1. **项目02**：切片碎片化 —— 按标题切分产出大量短碎片（3000字→19片，平均224字符）。
   加了 `_merge_short_chunks()` 合并过短相邻片段，优化到 11 片、平均 338 字符
2. **项目03**：模型幻觉参数导致崩溃 —— 模型传 schema 外字段时 `**arguments` 透传抛 TypeError。
   加了 `_filter_params()` 过滤
3. **项目03**：JSON 解析失败时未记录到 `step.observation`，导致调试看不到

测试结果：项目02 通过 5 组、项目03 通过 10 组（ReAct 循环）、项目04 通过 10 组 + 真实启动验证（uvicorn 起来、/api/health 返回正常、/docs 可访问）。

### 环境注意
- 沙箱环境对 `mkdir(exist_ok=True)` 处理有兼容问题，代码里改成先判 `exists()` 再 mkdir
- heredoc 里含 `sudo` 等字符串会被安全策略误判为系统级工具，测试代码需写成文件再跑

### 用户偏好观察
- 项目要能直接跑（含 .env.example、依赖文件、README 运行说明）
- 重视"为什么"而不只是"怎么做"
- 要求诚实标注信息来源和时间点
